# CONTEXT

本项目（公众号 Markdown 编辑器）的领域术语表。只记录领域语言，不记录实现细节。

## 核心概念

### 分享（Share）
一篇被发布出去的 Markdown 文章，拥有独立的查看链接（`/s/<id>`）。分享是内容的原子单位。

### 项目（Project）
一组分享的命名集合，拥有自己的聚合链接。项目是一个可选的容器：分享可以不属于任何项目（独立单篇分享），也可以属于一个项目。

**规则：**
- 一篇分享**最多属于一个项目**（不允许同一篇分享出现在多个项目中）。
- 项目可以为空。
- 创建分享不要求先创建项目。

### 读者（Reader）
通过链接查看分享或项目内容的人，无需任何身份认证。

### 发布者（Publisher）
通过编辑器页面或 skill（publish.py）创建分享、管理项目的人。目前为站长本人及其授权的 Agent。

### 项目链接（Project Link）
项目的聚合链接（`/p/<project-id>`）。打开后显示目录 + 项目中最新的一篇分享。

### 深链（Deep Link）
指向项目内某一指定篇分享的链接（`/p/<project-id>/<share-id>`）。读者打开深链时，既能看到该篇内容，也能看到整个项目的目录。读者在目录中切换文章时，地址栏同步变为当前篇的深链。

### 挂载（Attach）
把一篇分享归入某个项目的动作。三个入口：
1. **发布时挂载**：发布分享的请求里直接带上项目名；若项目不存在则**自动创建**（Agent 无人值守场景的关键语义）。
2. **编辑器发布时选择**：网页编辑器分享时可选已有项目或新建，默认为独立单篇。
3. **事后挂载/移动**：在管理界面中把已发布的分享挂入项目，或在项目间移动（一篇分享同时只属于一个项目，移动即改归属）。

### 令牌（Token）
站长签发的访问凭证，每个令牌有名字（如"我自己"、"巡检 Agent"、"张三"），用于发布分享和一切项目写操作。令牌可单独吊销。令牌替代了原先单一管理密码在 API 层的角色；读者查看内容不需要令牌。

### 创建者（Creator）
每篇分享、每个项目归属于创建它的那个令牌。管理界面可按创建者过滤。

### 项目命名规则
项目名在**同一创建者内部唯一**；不同创建者可以有同名项目，互不干扰。Agent/发布方按名字引用项目时，解析范围是自己（所持令牌）名下的项目。

### 管理页（Admin Page）
`/list` 升级后的管理后台。站长主密码或任一有效令牌均可登录：令牌登录只见自己名下的项目与分享；主密码见全部内容，且独占令牌管理区（签发/吊销）。
